# =============================================================================
# ci.yml — fast checks on every PR.
# =============================================================================
#
# This pipeline gates merges. It is intentionally split from release.yml so
# every PR runs the cheap, fast checks without dragging in the
# code-signing / notarization machinery (which is slow, secret-gated, and
# arm64-only).
#
# WHAT IT CHECKS
#   - cargo fmt --check          formatting drift in Rust code
#   - cargo clippy -D warnings   new lint warnings fail the build
#   - cargo test                 unit + integration tests for src-tauri
#   - pnpm run typecheck         vue-tsc --noEmit (strict)
#   - pnpm run lint              eslint flat-config (Vue 3 + TS)
#   - pnpm run format:check      prettier --check
#
# RUNNER CHOICE
#   We use macos-latest because src-tauri pulls in Apple-specific dependencies
#   (whisper-rs/metal, smappservice-rs, cocoa, etc.). Building on Linux would
#   require cfg-gating everything; not worth it until we actually target Linux
#   (Phase 4 work).
# =============================================================================

name: ci

on:
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  frontend:
    name: Frontend checks
    runs-on: macos-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          # pnpm 11 (pinned via packageManager in package.json) require()s the
          # node:sqlite builtin, which only exists in Node >=22.13. Node 20
          # crashes setup-node's `cache: pnpm` store-path probe.
          node-version: 26
          cache: pnpm
      - name: Install JS dependencies
        run: pnpm install --frozen-lockfile
      - name: Typecheck (vue-tsc)
        run: pnpm run typecheck
      - name: ESLint
        run: pnpm run lint
      - name: Prettier check
        run: pnpm run format:check

  rust:
    name: Rust checks
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      # rust-toolchain.toml pins the channel + components; no need to install.
      - name: Cache cargo
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: ${{ runner.os }}-cargo-ci-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-ci-
      - name: Format check
        run: cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check
      - name: Clippy
        # The Phase 0 backlog has been paid down (commit 85c9b4c) and the
        # local `pnpm run rust:clippy` already runs with `-D warnings`, so CI
        # matches. New warnings now fail the PR.
        run: cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
      - name: Test
        run: cargo test --manifest-path src-tauri/Cargo.toml --all-features
